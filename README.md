# VegaDevCli

> Work in progress

CLI tool for Vega OS (Amazon's React Native variant) development. Wraps the various tools you need (Kepler, ADB, Metro, etc.) into one interface.

## Build

```bash
dotnet build
```

## Usage

```bash
vega help
```
